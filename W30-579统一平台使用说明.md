# Agent-loop W30/579 统一平台使用说明

## 当前状态

Agent-loop Web 是唯一产品入口。W30 保留原有执行链；579 已接入无界面 Catalog、执行网关、环境检查、任务、证据和报告流程，不需要也不会启动 `desktop_qt.py` 或其他 Qt/Tk 工作台。

工程和全 fake 离线链路已实现。579 实机业务动作仍默认关闭，完成受控 Canary 并取得明确授权前，环境中心和运行接口会返回真实门禁原因，不会偷偷改用 W30 逻辑。

## 网页操作

1. 启动现有 Agent-loop Web 服务并打开 `http://127.0.0.1:8765/`。
2. 点击顶部“新增项目”，填写项目名称和 ID，选择允许的平台、执行目标及默认项。
3. 点击顶部“当前项目”可搜索并快捷切换；切换会保留当前主页面，但清空上一项目的用例选择。
4. 进入“用例管理”，上方“平台视图”决定当前查看的平台适配状态。
5. 勾选用例后，在固定执行栏选择“运行平台”和具体“执行目标”。
6. 前端读取后端 `execution-options` 显示可运行数量和阻塞原因；点击运行后，后端会重新校验 Project、Platform、Target、用例映射和环境门禁。

项目、平台和目标是三个独立概念。一个项目可允许 W30、579 或二者；只有用例在所选平台具有可运行映射时才能正式执行，失败时不自动回退到另一平台。

## 579 执行与证据链

```text
环境预检和 AUTO_READY 门禁
  -> 状态准备
  -> COM3/O1 先开始只读观察
  -> ADB -> APP Bridge -> BLE 下发动作
  -> O2 先进入截图观察，再触发截图
  -> 视觉/日志判定
  -> teardown 与状态恢复
  -> 统一历史、证据和报告
```

- COM3 只允许 `read/observe/capture/decode`，不存在 `write/send/flush` 接口。
- APP Bridge ACK 只说明动作交付，不能单独证明产品 PASS。
- O1 和新鲜 O2 截图证据不完整时，结果为 `CANNOT_VERIFY` 或基础设施异常。
- Web 和 Agent 边界不接收也不回显 579 原始 `cmd_id/key_id/data_hex`。
- 历史页分别显示产品结果、自动化成熟度和基础设施状态。

## 579 环境配置

在“环境中心”切换到 `579_O2`，可配置 ADB、APP Bridge、COM3 只读端口和证据目录。普通网页配置不能修改实机动作总门禁。

关键环境变量见 `.env.example`：

- `PLATFORM_579_ENABLED=false`
- `PLATFORM_579_DEVICE_ACTIONS_ENABLED=false`
- `PLATFORM_579_ADB_PATH`
- `PLATFORM_579_APP_PACKAGE`
- `PLATFORM_579_BRIDGE_COMPONENT` / `PLATFORM_579_BRIDGE_ACTION`
- `PLATFORM_579_COM_PORT=COM3`
- `PLATFORM_579_COM_BAUDRATE=1500000`

只有受控实机验收时，才可由授权人员在 Web 之外显式设置 `PLATFORM_579_DEVICE_ACTIONS_ENABLED=true`。

## 离线验证与构建

```powershell
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe tools\build_compat_wheel.py --output-dir .runtime\package-check --allow-dirty
.\.venv\Scripts\python.exe tools\verify_compat_wheel.py .runtime\package-check\agent_loop_system-0.4.0-py3-none-any.whl
```

正式发布必须在干净提交上去掉 `--allow-dirty`。校验器会确认 wheel 包含平台注册表、579 Catalog、动作绑定、状态恢复数据和无界面网关，并拒绝桌面 UI 文件与跨仓绝对依赖。
